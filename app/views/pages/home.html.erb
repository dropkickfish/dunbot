<% name = current_user.present? ? current_user.username : 'stranger' %>
<div>
    <h1>Hello, <%= name %></h1>
<div>
<%if current_user%>
 <div>
    <h2>Votes in progress</h2>
    <p>Oh look, it's fucking nothing</p>
    <h2><%= link_to 'Create a new vote', new_vote_url %></h2>
    <h2>Past votes</h2>
    <ul>
        <li>
        <li>
    </ul>
 </div>
<% end %>